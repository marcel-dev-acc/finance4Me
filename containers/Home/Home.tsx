import React, {type PropsWithChildren} from 'react';
import {
    SafeAreaView,
    ScrollView,
    View,
    Text,
  } from 'react-native';

const Home = () => (
   <SafeAreaView
   >
    <ScrollView>
        <View>
            <Text>Home</Text>
        </View>
    </ScrollView>
   </SafeAreaView>
);

export default Home;